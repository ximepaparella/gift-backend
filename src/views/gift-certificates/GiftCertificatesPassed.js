import { Card, CardHeader, CardBody, CardTitle, CardText } from 'reactstrap'

const GiftCertificatesPassed = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>GiftCertificatesPassed</CardTitle>
      </CardHeader>
      <CardBody>
        <CardText>Listado de Vouchers Vencidos</CardText>
      </CardBody>
    </Card>
  )
}

export default GiftCertificatesPassed
