import { Card, CardHeader, CardBody, CardTitle, CardText, CardLink } from 'reactstrap'

const SettingsPage = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Configuraciones</CardTitle>
      </CardHeader>
      <CardBody>
        <CardText>Modo de Pago, Mails</CardText>
      </CardBody>
    </Card>
  )
}

export default SettingsPage
