import { Card, CardHeader, CardBody, CardTitle, CardText, CardLink } from 'reactstrap'

const ClientList = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>ClientList</CardTitle>
      </CardHeader>
      <CardBody>
        <CardText>Listado de Clientes</CardText>
      </CardBody>
    </Card>
  )
}

export default ClientList
