import { Card, CardHeader, CardBody, CardTitle, CardText, CardLink } from 'reactstrap'

const MyAccountPage = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Mi Perfil</CardTitle>
      </CardHeader>
      <CardBody>
        <CardText>configuraciones</CardText>
      </CardBody>
    </Card>
  )
}

export default MyAccountPage
